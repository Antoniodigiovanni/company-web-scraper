[project]
name = "company-web-scraper"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "curl_cffi>=0.7",
    "trafilatura>=1.12",
    "lxml",
    "pandas",
    "playwright",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]
package = false
